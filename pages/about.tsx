import type { NextPage } from "next";
import Head from "next/head";

const About: NextPage = () => {
  return (
    <div>
      <Head>
        <title>About Us</title>
        <meta name="description" content="CMDT About Page" />
        <link rel="icon" href="/favicon.ico" />
      </Head>
    </div>
  );
};

export default About;
